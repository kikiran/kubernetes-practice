apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-deployment
  namespace: deployment-demo
  labels:
    app: bluegreen-demo
    version: blue
spec:
  replicas: 5
  selector:
    matchLabels:
      app: bluegreen-demo
      version: blue
  template:
    metadata:
      labels:
        app: bluegreen-demo
        version: blue
        color: blue
    spec:
      containers:
      - name: app
        image: YOUR_REGISTRY/deployment-demo:v1
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3000
        env:
        - name: APP_VERSION
          value: "v1"
        - name: APP_COLOR
          value: "blue"
        - name: DEPLOYMENT_TYPE
          value: "blue"
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
